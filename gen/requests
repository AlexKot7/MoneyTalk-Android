sign_up(phone)
session()
mobile_save_pin(pinHash, currentPinHash)
mobile_auth(pinHash, oldSessionId)
get_confirmation_code(user, initialOperationTicket, user_session_id)
transfer_any_card_to_any_pointer(srcAccountId, srcNetworkId, srcName, dstAccountId, dstNetworkId, dstName, moneyAmount, currency, message, image, invoice, ttl, cardId, cardNumber, expiryDate, securityCode)
transfer_any_card_to_any_card(cardNumber, expiryDate, securityCode, cardId, toCardNumber, toCardId, moneyAmount, currency, name, attachCard, cardName, screenSize, timezone)
attach_card(cardNumber, expiryDate, securityCode)
detach_card(cardId)
accounts_flat()
set_linked_card_primary(cardId)
payment_commission(cardId, cardNumber, paymentType, provider, currency, moneyAmount, fielddstPointerType, fielddstPointer, fieldtoCardNumber)
payment_receipt()
now()
ping()
confirm(secretValue, confirmationType, initialOperationTicket, initialOperation)
config()
reset_wallet()
resendCode(initialOperationTicket, confirmationType, initialOperation)